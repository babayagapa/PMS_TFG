/**
 * ReceptionistReservations – Reservation management for the Recepcionista role.
 *
 * Accessibility (WCAG 2.1 Level A):
 *  - Search input has a visible label.
 *  - Table uses scope attributes for headers.
 */
export default function ReceptionistReservations() {
  // TODO: Fetch from GET /api/reservations and implement create/edit/cancel UI
  return (
    <main className="min-h-screen bg-gray-100 p-6">
      <h1 className="mb-4 text-2xl font-bold text-hotel-night">Reservas</h1>
      <p className="text-gray-500">Implementación pendiente.</p>
    </main>
  );
}
