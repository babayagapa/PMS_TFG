import { calcularPrecio } from '../utils/calcularPrecio'

// Formulario de reserva con cÃ¡lculo de precio en tiempo real
export default function ReservaForm({ habitacion, onSubmit }) {
  // TODO: campos nombre, email, telÃ©fono, fechas, huÃ©spedes
  // TODO: mostrar resumen: X noches Ã— Yâ‚¬ + IVA = total
  return <div>TODO: formulario reserva</div>
}